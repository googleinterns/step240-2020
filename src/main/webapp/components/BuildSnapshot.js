import * as React from "react";

/**
 * Component used to render Build Information.
 * @param {*} props input properties containg build information
 */
export const BuildSnapshot = (props) => {
    return (
        <div>
            Snapshot
        </div>
    )
}
